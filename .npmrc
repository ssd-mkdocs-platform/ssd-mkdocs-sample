# pnpm configuration
# Disable pre/post scripts by default
# Only packages listed in package.json's "pnpm.onlyBuiltDependencies" will run build scripts
enable-pre-post-scripts=false

# Workaround for Windows EPERM error caused by antivirus blocking atomic file copies
# node-linker=hoisted
